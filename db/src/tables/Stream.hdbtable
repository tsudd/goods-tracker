COLUMN TABLE "STREAM" (
    "ID" INTEGER NOT NULL COMMENT 'ID of item stream',
    "FETCH_DATE" TIMESTAMP COMMENT 'Moment when stream was wetched',
    PRIMARY KEY ("ID")
) COMMENT 'Stream' WITH ASSOCIATIONS (
    JOIN "ITEMRECORD" AS "RECORDS" ON "RECORDS"."STREAMID" = "ID"
)