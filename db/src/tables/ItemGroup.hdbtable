COLUMN TABLE "ITEM_GROUP" (
    "ITEM_ID" INT COMMENT 'Item ID',
    "GOOD_ID" INT COMMENT 'GOOD ID',
    PRIMARY KEY ("ITEM_ID", "GOOD_ID")
) COMMENT 'GOOD Link' WITH ASSOCIATIONS (
    JOIN "GOOD" AS "GOODS" ON "GOODS"."ID" = "GOOD_ID",
    JOIN "ITEM" AS "ITEMS" ON "ITEMS"."ID" = "ITEM_ID"
)